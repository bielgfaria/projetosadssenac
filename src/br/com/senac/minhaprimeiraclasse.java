package br.com.senac;

public class minhaprimeiraclasse {

    public static void main(String[] args) {
      /* decimal --> float
         inteiro --> int
         decimal velocidadeInicial = 40.0;
         decimal velocidadeFinal = 0.0;
         decimal desaceleracao = -4.0;
         psvm + tab = publicstaticvoidmain*
         decimal tempo = (velocidadeFinal - velocidadeInicial / desaceleracao
         Imprimir (tempo)
         DECIMAL = double* obs: shift+f6 executa o codigo.
         alt+shift+f formata os textos selecionados.
         sout = system.out.print.
         ctrl+espaço completa a variavel ex:cel vira celsius automatico.
         ctrl + x = apaga a linha.
         alt+shift + setas = sobe e desce linhas.
         ctrl+shift+seta inferior = duplica linha pra baixo.
         Formas de se declarar:
         -Sem inicicialização:
         iten = quantidadeItens;
         double = nota2, valorCalculo, somaProduto;
         -Com inicialização:
         int quantiodadeProdutos = 0;
         String mensagem = "olá";
         boolean = usuarioCadastrado = false;
         int qtdProduto = quantidadeProdutos;
         ctrl+shift+i = atalho importar.
        int tamanho = nome.lenght(); (obs: da o tamanho do texto qtdade de caracteres)
        string nome =   \n quebra linha \n\t quebra linha e da um tab
         */

        double vInicial = 40.0;
        double vFinal = 0;
        double desacel = -4;
        double tempo = (vFinal - vInicial) / desacel;
        System.out.println(tempo);

    }

}
